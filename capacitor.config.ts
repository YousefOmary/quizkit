import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yousefomary.atlassprint',
  appName: 'Atlas Sprint',
  webDir: 'dist',
};

export default config;
